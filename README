This is README file for explain how to build your Python module package.

Usage:

from afs.client import afs
client = afs()
client.models.upload_model('model.h5', accuracy=0.4, loss=0.3, tags=dict(machine='machine01'))
